<!DOCTYPE html>
<html lang="es">
<%- include('../partials/head') %> 
<head>
  <link rel="stylesheet" href="/css/productDetail.css" />
  <title>Pegasus Tech | Producto</title>
</head>

<body> 
  <%- include('../partials/header') %>

  <main class="product">
    <div class="img">
      <img src="<%= uploadsPath + '/products/' + productInfo.images %>" alt=""/>
    </div>
    <div class="product-info">

      <div class="product-category"><p>Categoría: <%= productInfo.category.name %> </p></div>
      <div class="product-name"><h1><%= productInfo.name %> </h1></div>
      <div class="product-price"><h3>$ <%= productInfo.price %> </h3></div>
      <div class="product-description"><p><%= productInfo.description %> </p></div>
      <div class="product-stock"><h5>Stock: <%= productInfo.stock %> </h5></div>
      
      <div class="form-buttons">
        <button type="button" form="addToCart" class="btn main-button">Agregar al Carrito</button>
        <% if (locals.isLogged && locals.userType == "Admin") { %>
          <a href="/products/productEdit/<%= productInfo.id %>" class="btn">Editar Producto</a>
        <% } %> 
      </div>
    </div>
  </main>

  <!-- <h2 class="section-title degradado">Recomendados</h2>
  <section class="recopilacion">
    <div class="producto-preview"></div>
    <div class="producto-preview"></div>
    <div class="producto-preview"></div>
    <div class="producto-preview"></div>
  </section>

  <section class="recopilacion historial" style="display: none">
    <div class="producto-preview"></div>
    <div class="producto-preview"></div>
    <div class="producto-preview"></div>
    <div class="producto-preview"></div>
  </section>-->
  
  <%- include('../partials/footer') %>
</body>
</html>