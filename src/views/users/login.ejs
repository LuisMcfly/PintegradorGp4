<!DOCTYPE html>
<html lang="es">
<%- include('../partials/head') %>
<!-- <head><link rel="stylesheet" href="/css/login.css" /></head> -->

<body> 
  <%- include('../partials/header') %>

  <main class="form-container">
    <form action="/users/login" method="POST" id="loginForm">
      <section class="form-section">
        
        <h1>¡Bienvenido de nuevo!</h1>

        <div class="form-field">
          <label for="email"><p>E-mail</p></label>
          <input type="text" name="email" id="email" class="text-input">
          <% if(locals.errores && errores.email) {%> 
            <div class="error-message">
              <p> <%= errores.email.msg %> </p>
            </div>
          <% }; %> 
        </div>
        <div class="form-field">
          <label for="password"><p>Contraseña</p></label>
          <input type="password" name="password" id="password" class="text-input">
          <% if(locals.errores && errores.password) { %>
            <div class="error-message">
              <p> <%= errores.password.msg %> </p>
            </div> 
          <% } %>
        </div>

        <div class="form-field">
          <div class="check-container">
            <input type="checkbox" name="rememberMe" id="rememberMe" 
            class="checkbox-input with-font" form="registerForm">
            <label for="rememberMe" class="icon paragraph"> Recuérdame </label>
          </div>
        </div>

        <div class="form-buttons">
          <button type="submit" class="main-button">Ingresar</button>
        </div>
        
        <div class="form-footer">
          <a href="#" class="olvide-contrasena"> Olvidé mi contraseña </a>
          <p>¿Necesitas una cuenta? <a href="/users/register">Registrate</a></p>
        </div>
      </seciton>
    </form>
  </main>
  <%- include('../partials/footer') %>
</body>
</html>